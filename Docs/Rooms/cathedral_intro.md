# 房间规格：大教堂 · 序章侧厅（Cathedral Intro）

教堂可玩切片的**第一间**。职责只有一件：从裂隙爬起 → 空手学走跳与危险 → 从没收架夺回残誓 → 出门进入中殿/确认手感房。

相关文档：

- 叙事节拍：[Script.md](../Script.md)
- 技能与序章规则：[Skills and route design.md](../Skills%20and%20route%20design.md)
- 武器造型与阶段：[Art/protagonist_weapon_spec.md](../Art/protagonist_weapon_spec.md)
- 落地步骤：[implementation_steps.md](../implementation_steps.md)

---

## 0. 实现进度（活文档）

| 区段 | 状态 | 入口 / 备注 |
|------|------|-------------|
| **A 裂隙坑** | **进行中（可玩）** | `CathedralIntroZoneA`；getup、死墙、裂隙纵深、**连续地面+顶棚**、圣水气氛+伤害；**SV1 后置** |
| B 圣水走廊 | 未做 | 仅 A 右侧地板 stub |
| C 没收龛 | 未做 | — |
| D 出口拱 | 未做 | — |

**A 段临时读档规则：** 尚无正式存档祭台；死亡 / 掉虚空回 `PlayerController.SpawnPosition`（出生点）。正式 `SaveShrine`（SV1/SV2）与序列化读档锚点**后置**，见 §3.A / §7。

---

## 1. 已拍板结论

| 项 | 锁定 |
|----|------|
| 场景归属 | **`Cathedral_Hub` 同场景子区**（墙体隔开的左侧底层侧厅）；一期不做独立 `Cathedral_Intro.unity`，避免多一次加载 |
| 残誓叙事 | **没收架**：墙龛铁架挂裂纹短剑，褪红布签「不洁证物」；交互夺回 |
| 存档 | **两处（设计锁定，落地后置）**：① 出生点旁 SV1 ② 拾剑后、出口门前 SV2；`save_cathedral_*` |
| 战斗 | **本房零敌人**；攻击确认放到出口右门外的下一间 |
| 门控 | 无能力门；空手可走完全程并出门（软引导拾剑，可不硬锁出口） |
| 时长 | 空手段约 **1～2 分钟**；整段序章（含拾剑）约 **2～4 分钟** |
| 相机 | 单层为主；宽约 **1.2～1.5 个 Orthographic 视野**（参考 Size 6～8） |

**一句话：** 墓穴裂隙爬进圣堂的第一口气——不被欢迎，夺回未交还的誓，再踏入中殿。

布局概念图（侧视四拍参考，非最终碰撞）：

![Cathedral Intro 布局概念](./cathedral_intro_layout_concept.png)

---

## 2. 与样板大殿的分工

| 空间 | 职责 |
|------|------|
| **本房 · Intro 侧厅** | getup、空手教学、没收架残誓、双存档、单出口 |
| **右侧 · Sample 中殿**（同场景稍后区域） | 枢纽：梯、裂隙门、净光障壁景观、通往冲刺/Boss；**不承担**空手序章 |

视觉参考：`map_room_sample_cathedral.png` 的**左下角地板 + 单柱 + 单拱门**气质；碰撞自描，**不要**把整张样板当第一间（出口过多）。

---

## 3. 空间节奏（左 → 右）

四拍连续，建议总宽约 **28～36 世界单位**（按角色高约 2、相机 Size 6～8 调）。

```
[A 裂隙坑] → [B 圣水走廊] → [C 没收龛] → [D 出口拱]
  getup+圣水      走/跳/刺         残誓+存档2      进中殿
  （SV1 后置）
```

### A · 裂隙坑（出生）

| 元素 | 规格 | 落地 |
|------|------|------|
| Spawn | 坑底裂纹平台；开场播 **getup**，控制锁至结束 | ✅ |
| 裂隙 | 通向「地下」的视觉裂口（一期不进墓穴）；身后**厚石封墙**（填块体积 + 立面墙板 + 铁栅 + blocker），接地面/顶棚 | ✅ |
| **地面** | 统一完整石砖（`map_tileset_cathedral_1`）铺满可走面；廊底有垫层，坑下仍留裂隙虚空 | ✅ |
| **房顶** | 连续檐口；生成后可在 Scene 手调，Ctrl+S 保存 | ✅ |
| **圣水** | 见下表「圣水规格」；**气氛 + 单点伤害**（已拍板） | ✅ |
| **存档 1（SV1）** | 起身可交互范围内；教第一次存档 / 读档 | ⏸ **后置**；现阶段死回出生点 |

#### 圣水规格（A · 已锁定实现）

职责是**叙事**（教堂不欢迎朝圣者），不是走廊教学。

| 项 | 锁定 |
|----|------|
| 摆法 | 顶棚代码铺无缝中段变体 `cathedral_15/b/c`（略重叠）；圣水 X 同排换同规格 `holywater_ceiling_block` 对准 `holywater_basin_pool` |
| 伤害 | 踩中 **1** 点；满血 3；可绕开；**勿整坑持续掉血** |
| 触发 | `Hazard` 层 Trigger；`DamageZone`；getup / 控制锁期间不伤 |
| 反馈 | 短击退 + ~0.85s 无敌帧 + 闪烁；首次命中旁白一句 |
| 旁白 | 「圣水灼肤——这里不欢迎他/她。」（一期 Console / 日后 UI，只播一次） |
| 美术 | 顶棚：`cathedral_15` / `15b` / `15c` 无缝中段；滴水：`holywater_ceiling_block`；地面水洼：`holywater_basin_pool` |
| 脚本 | `PlaceHolyWater` / `DamageZone` / `PlayerHealth`（场景手摆；可选重置生成） |

### B · 圣水走廊（空手教学）

只各教一次，**禁止**必须普攻才能过的障碍：

| 教学 | 摆法 |
|------|------|
| 走 | 平坦石廊 6～10 单位 |
| 跳 | 一道小缺口或矮台（单跳可过） |
| 危险 | 短条 `spike_floor_cathedral`（可绕可跳）；勿铺满整廊 |

可选：侧上方**看得见但跳不过**的高台，放空鞘 / 锈剑剪影（诱饵），真正的残誓在 C。

> A 的圣水灼点**不替代** B 的地刺教学；B 用地刺复用同一套 `DamageZone` / 无敌帧。

### C · 没收龛（房间心脏）

| 元素 | 规格 |
|------|------|
| 焦点 | 比走廊亮一档（光柱或龛灯）；远看可辨「去那里」 |
| **没收架** | 墙龛铁架；挂 **污光断剑·残誓**（抗拒态：裂纹暗淡）；褪红布签「不洁证物」 |
| 交互 | 走近按交互 → 短停顿（可无字或一行「未交还的誓。」）→ 斜挂胯侧 → `HasOathblade = true` |
| **存档 2** | 拾剑点右侧数步、出口门前；拿剑后第一次稳妥落脚点 |

拾取时本房仍无怪，避免演出被打断。

### D · 出口拱

| 元素 | 规格 |
|------|------|
| 门 | 向右普通拱门 → 同场景中殿入口，或短过渡到「确认手感」小区 |
| 未拾剑 | 门**可开**（软引导）；不硬锁。若需强引导，仅用路牌/光，不关门 |
| 门外 | **下一拍**（可同场景邻接）：1 只碎誓侍僧占位或可砍圣靶；此处才第一次开 Attack |

左侧或上层：假门 / 封墙 / 透过镂空瞥见中殿玫瑰窗光柱——种「先见后至」，本房走不到。A 左侧铁栅封门已占位；上层窥中殿可后补。

---

## 4. 高度与碰撞

- **主路径单层**；最多 1～2 个矮跳台（高度约 1 次跳跃）。
- 不要塞样板图右侧盘旋楼梯。
- 地面连续可走（砖缝碰撞略重叠，防掉缝）；裂隙坑用平台托住，防止开场掉虚空（或虚空触发回 Spawn）。
- Zone A 有连续顶棚（代码铺）；圣水 X 同排为滴水块，高度约 `floorTop + 4.2`。
- 碰撞层：地面 `Ground`；圣水灼点 / 刺 `Hazard`；存档/没收架 `Interact`；角色不与装饰 props 硬碰（翅膀/披风无碰撞）。

---

## 5. 物件摆放表

| ID | 物件 | 区段 | 资源 / 占位 | 作用 | 状态 |
|----|------|------|-------------|------|------|
| S0 | Player Spawn | A | — | getup 起点 | ✅ |
| HZ0 | 圣水灼点 + 顶棚滴水块 | A | `basin` + `cathedral_15` 顶棚 + `ceiling_block` 同行 | 气氛；1 伤可绕 | ✅ |
| SV1 | 存档祭台 | A | `map_interact_save_cathedral` 熄/燃 | 教交互；死亡回最近激活档 | ⏸ 后置 |
| HZ1 | 地刺短条 | B | `spike_floor_cathedral`（`map_hazards_cathedral`） | 教伤害/击退 | ❌ |
| PL1 | 矮平台 ×1～2 | B | `map_tileset_cathedral` 块 | 教跳 | ❌ |
| WP1 | **没收架 + 残誓** | C | 交互物；剑可用 `stained_oathblade_concept` 缩小占位 | 解锁 `HasOathblade` | ❌ |
| SV2 | 存档祭台 | C→D | 同上 | 拾剑后安检点 | ⏸ 后置 |
| DR1 | 出口拱门 | D | `door_basic_open` 或无脚本门洞 | 进中殿 / 确认房 | ❌ |
| FX1 | 假门或封墙 | A 左 / 上层 | props 铁栅/砖封 | 暗示后续路线 | ✅ 左封门；上层后补 |
| PR* | 跪天使、褪红旗、碎柱 | 各段 | `Props/cathedral/` | 纯装饰，不挡主路 | ✅ A 段已摆 |

环境资源目录（重组后）：

- 背景：`Assets/Art/Environment/Backgrounds/`
- 地块：`Assets/Art/Environment/Tilesets/`
- Props：`Assets/Art/Environment/Props/{cathedral,cavern,clocktower,crypt,forest}/`
- 危险：`Assets/Art/Environment/Hazards/`
- 交互：`Assets/Art/Environment/Interact/`

侧厅可用样板图局部或纯色石墙 + props，不必整开样板碰撞。

---

## 6. 残誓来历（本房露出层）

本房只露**表层**；完整四拍见武器规范「来历」。

| 层 | 本房是否出现 |
|----|----------------|
| 表层：祈福刃被登记为不洁证物、挂在没收架 | **是**（布签 + 拾取一行字） |
| 中层：批量发放给「可净化者」、裂刃者驱逐 | 否（教堂中段/Boss 前） |
| 深层：琥珀与古朝圣者同源 | 否（墓穴后） |

拾取文案（锁定短句，可择一）：

- 「未交还的誓。」
- 「裂纹还在。」

物品栏短描述（可后做）：  
> 本为交还圣域的祈福刃；誓碎于净化之日，裂纹成诅咒证物。

---

## 7. 玩法与脚本接口（一期）

| Flag / 组件 | 行为 | 状态 |
|-------------|------|------|
| `HasOathblade == false` | 忽略 Attack 输入；无空手攻击动画 | 设计锁定；C 段落地 |
| 交互没收架 | 设 `HasOathblade = true`；可选播短持刃/挂鞘演出 | ❌ |
| `SaveShrine` ×2 | 写入场景、位置、能力旗（含残誓）、钥匙等 | 脚本壳有；**本房未挂**（后置） |
| `DamageZone`（圣水 / 日后刺） | 伤害 + 短击退 + 无敌帧；死则回最近存档（现回 Spawn） | ✅ A 圣水 |
| 出口 | 同场景走过去即可；若拆门脚本则 Fade 可后补 | ❌ |

**不要**在本房做：冲刺教学、钥匙门、能力门、侍僧战斗、净光障壁。

---

## 8. 验收自检

### A（当前可验）

- [x] 开场 getup 后只能走/跳
- [x] 裂隙纵深 + 左厚石封墙可读（非背景贴条）
- [x] 地面统一完整石砖、连续无掉缝；廊底有垫层（坑下仍见裂隙）
- [x] 连续顶棚；圣水滴水块对准灼点
- [x] 顶棚为 `cathedral_15` 变体；圣水 X 同排为 `holywater_ceiling_block`；地面 basin 灼点可踩
- [x] 灼点可绕；踩中 1 伤 + 击退 + 无敌；首次旁白一句
- [x] 掉虚空 / 死透回出生点（无 SV1 时）
- [ ] SV1 可存可死可读回裂隙旁（后置）

### 整房（待 B/C/D）

- [ ] 按攻击无效果（至拾剑前）
- [ ] B 刺可绕过；无一处逼攻击清路
- [ ] C 没收架可读为「赃物/证物」；拾取后 Attack 可用
- [ ] C→D 第二存档可激活；拾剑后踩刺死亡可读回此处
- [ ] 本房零敌人；出门后才遇怪或靶
- [ ] 假门/高处诱饵不可达，但不像关卡漏洞
- [ ] 与中殿同场景：向右连续进入枢纽，无多余加载闪一下（一期）

---

## 9. 实现备注

- 内部区域名：`Cathedral_Hub / Zone_Intro`；组件 `CathedralIntroZoneA`。
- **摆关方式（场景即真相）：** 在 Hierarchy / Scene 直接拖地板、顶棚、人物、圣水 → **Ctrl+S 保存场景**。日常不需要 Bake / Rebuild。
- 空场景可点 **首次生成**；只有要推倒重来时才用 **重置生成**（会清空手改）。
- **导出备份** 可选，写入 `ZoneA_Layout.json` 作保险 / 重置种子。
- 美术：顶棚可用生成器铺 `cathedral_15` / `15b` / `15c` + `holywater_ceiling_block`，之后在场景里手调并保存。
- 比例目标：chibi 站在侧厅地板上像「人在侧廊」，不要蚂蚁感；角色 sheet PPU **256**，角色高约 2；环境 hazard sheet PPU **100**。
- 建议下一刀：**SV1**，或铺 **B 走廊**（走/跳/刺，复用 `DamageZone`）。
- 做完本房验收后，再铺右侧中殿的裂隙门 / 净光障壁「先见后至」。
